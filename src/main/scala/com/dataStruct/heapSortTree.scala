package com.dataStruct

object heapSortTree {
  def main(args: Array[String]): Unit = {
     // val arr:Array[Int]={}
  }

  /**
    * 实现二叉树组合 完全二叉树
    * arr 目标元素
    * n   表示父节点
    */
  def heapfiy(arr:Array[Int],n:Int):Unit={
    val c1 = 2*n+1
    val c2 = 2*n+2
    if (c1>n && arr(c1)>arr(n)){

    }

    if (c2>n && arr(c2)>arr(n)){

    }


  }
}
